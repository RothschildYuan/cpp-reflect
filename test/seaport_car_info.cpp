#include "seaport_car_info.h"

seaport_car_info::seaport_car_info()
{
    INIT_VALUE(id);
    INIT_VALUE(optor_id);
    INIT_VALUE(optor_code);
    INIT_VALUE(optor_name);
    INIT_VALUE(car_no);
    INIT_VALUE(car_color);
    INIT_VALUE(car_type);
    INIT_VALUE(car_length);
    INIT_VALUE(car_width);
    INIT_VALUE(car_height);
    INIT_VALUE(car_weight);
    INIT_VALUE(crea_time);
}
